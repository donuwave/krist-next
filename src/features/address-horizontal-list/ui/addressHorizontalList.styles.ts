import { styled } from 'styled-components';

export const STitle = styled.h4`
  font-weight: 700;
  font-size: 18px;
`;

export const SList = styled.div`
  display: flex;
  overflow-x: auto;
  overflow-y: hidden;
  gap: 18px;
  padding-bottom: 12px;
  margin: 24px 0;
`;
