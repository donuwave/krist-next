import { styled } from 'styled-components';

export const SCartPopover = styled.div`
  min-width: 320px;
  display: grid;
  gap: 8px;
`;

export const SSubTotal = styled.div`
  display: flex;
  justify-content: space-between;
  font-weight: bold;
  font-size: 16px;
  padding: 18px 0;
`;

export const SList = styled.div`
  display: flex;
  flex-direction: column;
  padding-top: 18px;
  gap: 12px;
`;
