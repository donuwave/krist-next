# krist-next

Арендовал сервер (VPS) с публичным IPv4-адресом 147.45.185.125.
Подключился к серверу по SSH (через терминал или Termius).
Установил на сервер необходимое ПО: Node.js, npm, PM2, Git, Nginx.
Развернул фронтенд (Next.js):

Склонировал репозиторий (git clone).
Установил зависимости (npm install).
Собрал проект (npm run build).
Запустил его через PM2 на порту 3000 и добавил в автозагрузку.
Настроил Nginx как Reverse Proxy:

Создал конфигурационный файл для своего сайта.
Прописал проксирование всех запросов с 80-го порта на локальный порт 3000, где работал Next.js.
Удалил дефолтный конфиг Nginx (default), чтобы он не мешал.
Привязал домен tareev.space:

Узнал IP своего сервера.
Зашёл в панель управления DNS регистратора (предположительно, Reg.ru).
Изменил (или создал) A-записи для @ и www, указав в них IP своего сервера 147.45.185.125.
Дождался обновления DNS — проверил через nslookup, что домен теперь резолвится в нужный IP.


host - https://server112.hosting.reg.ru:1500/ispmgr#/list?elid=tareev.space&elname=tareev.space&p_num=1&tab_parent_id=1&func=domain.record&tab_id=6
аренда - https://timeweb.cloud/my/servers/6714805
